import { createTheme } from "@mui/material";

export const materialTheme = createTheme({
  palette: {
    primary: {
      main: "#000000",
    },
    secondary: {
      main: "#00A76F",
    },
  },
});
